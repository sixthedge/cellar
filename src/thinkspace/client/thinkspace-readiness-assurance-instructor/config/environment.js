/*jshint node:true*/
'use strict';

module.exports = function(environment) {
  var ENV = {
    modulePrefix: 'thinkspace-readiness-assurance-instructor',
    environment: environment
  }

  return ENV;
};
