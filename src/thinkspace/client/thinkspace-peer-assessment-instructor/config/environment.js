/*jshint node:true*/
'use strict';

module.exports = function(environment) {
  var ENV = {
    modulePrefix: 'thinkspace-peer-assessment-instructor',
    environment: environment
  }

  return ENV;
};
